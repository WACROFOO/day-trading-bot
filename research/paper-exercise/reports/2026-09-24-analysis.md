# Session 2026-09-24 — first phase-C morning: two scratches, one desk outage, Layer 2 measured gate by gate

**Provenance.** Every number below is read from the owner's terminal output
of `exercise.py review` and `exercise.py missed --day 2026-09-24` (ledger
`data/journal.sqlite`, phase C, 9 sessions counted before today) and from the
desk log of the day, pasted the same evening. The day was NOT settled when
those commands ran: the desk lost IBKR at 10:40 ET and the after-close block
never executed, so today's `missed` rows carry no outcomes yet (dashes) and
the session is not counted. The Yahoo-bar table of the morning is not used
here. Upper bounds are labelled as such. Paper only.

## 1. What happened, in order (ET)

| time | event | source |
|---|---|---|
| 06:11–06:20 | three starts; the first two read `competing` (10197, a live login held the data), the third `realtime` | desk log |
| 06:20 | desk up, phase C, TRADE, pre-market entries ON (probe `queued`, A1 accepted) | desk log |
| ~07:15 | Gateway lost IBKR (1100), then the Gateway process itself went down (port 4002 refused, 10141 disclaimer); desk OFFLINE; Ctrl-C | desk log |
| 08:07 | restart; 7 plans 07:23–08:07 tagged backfill | desk log |
| 08:36 | second competing login (10197) named by the desk; data resumed on its own | desk log |
| 09:38 | **PFSA TAKEN** 3.73/3.49, fill 3.74 × 83; trail to 3.73 by 09:39:53 (high 3.97); stop executed 09:40:00 at 3.73 | ledger `orders` 7 |
| 10:07 | **GRML TAKEN** 14.47/14.02, fill 14.49 × 44; trail to 14.46 by 10:10:30 (high 14.91); stop executed 10:14:11 at 14.48 | ledger `orders` 8 |
| 10:38–10:40 | rebuild 20 s then 31 s, feed STALE, history timeouts, 1100 repeated; no position open | desk log |
| after 10:40 | the day command exited; no after-close block; session not counted | `review` header, `missed` dashes |

## 2. The trades

| # | sym | in | fill | qty | out | exit | reason | $ | R |
|---|---|---|---|---|---|---|---|---|---|
| 7 | PFSA | 09:38 | 3.74 | 83 | 09:40 | 3.73 | trail | −0.83 | −0.04 |
| 8 | GRML | 10:07 | 14.49 | 44 | 10:14 | 14.48 | trail | −0.44 | −0.02 |

Both fills sit inside the A10 offset (slippage ratios 1.04 and 1.04 against
0.55 for WHLR the day before): the stop-limit entry does what it was built
for. Both stops executed within one bar of the level: yesterday's failure
mode (a resting stop that did not execute for fifty minutes) did not recur.
Both trades ran about +1 R and reversed; the A3 trail locked breakeven both
times. Neither reached the fixed 2 R target (4.21 and 15.37), so the kill
rule's baseline would have read −1 R or a scratch on each; the graded
comparison comes with the settle. Cumulative: 6 filled, 6 closed, 2 won,
net −2.18 R planned.

The risk gate read `streak 2` on two scratches. That interaction between A3
and the consecutive-loss rule is recorded and changed in
`docs/preregistration.md` §5 (owner decisions of 2026-09-24).

## 3. What was refused today (prospective rows)

| reason | n |
|---|---|
| killed: price | 19 |
| killed: rising | 19 |
| Layer 2 not green (chart) | 9 |
| Layer 2 not green (volume) | 8 |
| bar outside the 07:00 window | 4 |
| one position at a time | 1 |
| A6 spread (GRML 09:52, 0.27 stop inside 4 × 0.10) | 1 |

Pre-market: 14 plans the chart decided, none all green (VWAP red 9, MACD red
8, volume red 7). No plan was ARMED; the runner-triggered pre-market entry
path has not fired yet. What each refused plan went on to do is graded at
the settle.

## 4. Layer 2 gate by gate, cumulative (graded days, upper bound: fill at trigger, no costs)

| window | cohort | n | fixed 2 R | A3 trail | win |
|---|---|---|---|---|---|
| pre-market | all four gates green | 13 | −4.00 | −4.51 | 23% |
| pre-market | MACD the only red gate | 8 | −3.00 | +2.67 | 17% |
| pre-market | volume the only red gate | 5 | +0.00 | −0.94 | 33% |
| regular | all four gates green | 16 | +6.35 | +12.48 | 54% |
| regular | MACD the only red gate | 9 | +7.00 | +6.61 | 62% |
| regular | volume the only red gate | 6 | +6.00 | +15.49 | 67% |
| regular | VWAP the only red gate | 9 | +3.00 | +0.13 | 50% |

Reading: in regular hours the rule set is positive on the plans it approves,
and the MACD gate and the volume gate are each, on nine and six rows,
refusing cohorts that scored positive on both exit rules. In pre-market the
fully approved cohort is negative and no single gate rescues it. The decision
rule for a first Layer 2 amendment is now written in the preregistration,
dated before the data that will decide it: 20 rows, both exit rules positive,
regular hours only, the all-green cohort positive. Pre-market is not touched
before five phase-C sessions are graded.

## 5. Defects and fixes shipped this evening

| observed | fix | where |
|---|---|---|
| rebuild 2 s → 31 s over the session; every slow rebuild followed a `headlines:` line | headline fetch off the rebuild path, own thread, merged on the worker | `IbkrDesk._pull_headlines` |
| rebuild cost grows with the square of the session | engine history queries walk back from the newest bar; answers unchanged | `SymbolState` |
| day exited before 11:30: no grading, no report, session not counted | settled at the next start, tape completed from IBKR first; `missed`/`review` grade in memory meanwhile | `day.settle_unsettled`, `controls.per_decision` |
| two scratches read as a two-loss streak | ±0.25 R is a scratch for the streak (owner decision, §5) | `src/journal/risk.py` |
| refusal text cut at 90 characters ("Layer 2 not green: ve") | every reason printed in short form | `exercise.py live` |
| `tape.py` on Yahoo while the desk ran on IBKR | IBKR whenever the Gateway answers, labelled | `scripts/ib_feed.py` (same day, earlier commit) |

Not fixed, not understood: the 1-minute pane for SRZN drew volume and MACD
but no candles, price axis 0.5–5.5, while the 5-minute and 10-second panes
drew the same tape. Needs the browser console.

## 6. My own calls, scored

- "PFSA's stop may have failed again" (09:49, before the ledger check): wrong. It executed at 09:40:00. The runner prints nothing on a broker fill, which is what made the silence ambiguous.
- "A fixed target would have been reached on neither trade": right on the levels (4.21, 15.37 never printed).
- The morning's Yahoo table (+10 R over 11 pre-market plans): an upper bound on bars that hide pre-market volume, superseded by the ledger's graded rows once the day settles.

## 7. Limitations

Upper bounds assume a fill at the trigger with no costs and ignore the
one-position rule. The regular-hours cohorts are 6 to 16 rows. Today's rows
are ungraded until `day.py --settle 2026-09-24` (or the next start) runs;
bars after 10:40 exist only if the backfill fetches them. Paper fills are
optimistic. Nothing here is a claim of edge.
