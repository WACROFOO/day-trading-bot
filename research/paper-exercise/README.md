# Paper exercise

The live record of `docs/paper-exercise-brief.md`. Nothing here is a claim
of edge; every report measures selection against the free baselines on the
same names at the same instants.

| what | where |
|---|---|
| the ledger itself (SQLite, gitignored) | `data/journal.sqlite` |
| one report per session day, written by `scripts/day.py` after 11:30 ET | `reports/` |
| the spec | `docs/paper-exercise-brief.md` |
| the pre-registered sample size, gates and failure condition | `docs/preregistration.md` |
| how to run a day | `docs/day-runbook.md` |
