# data/

Artefacts of the megaday study: the per-day tables the analysis in
`../RESULTS.md` was computed from.

These CSVs are the deliverable, not scratch — they are listed explicitly in
`.gitignore` so the blanket `*.csv` rule does not silently drop them.
